import { Diagnostics } from '../diagnostics';
import { RetryStrategy } from '../retry-strategy';
type RetryStrategyProps = {
    doubleCheck?: boolean;
    retryStrategy?: RetryStrategy;
};
export declare function validateDeprecatedDoubleCheck(diagnostics: Diagnostics, props: RetryStrategyProps): Promise<void>;
export declare function validateRemovedDoubleCheck(diagnostics: Diagnostics, props: RetryStrategyProps): Promise<void>;
export {};
