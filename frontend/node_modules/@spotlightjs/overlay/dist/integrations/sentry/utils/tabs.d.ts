import { IntegrationPanel } from '../../integration';

export declare function createTab<T>(id: string, title: string, extra?: Partial<Omit<IntegrationPanel<T>, "id" | "title">>): IntegrationPanel<T>;
