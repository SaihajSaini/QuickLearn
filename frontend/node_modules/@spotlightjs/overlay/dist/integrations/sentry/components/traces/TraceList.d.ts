import { Trace } from '../../types';

type TraceListProps = {
    traceData: {
        filtered: Trace[];
        all: Trace[];
        visible: Trace[];
        hiddenItemCount: number;
    };
    onShowAll: () => void;
};
export default function TraceList({ traceData, onShowAll }: TraceListProps): import("react/jsx-runtime").JSX.Element;
export {};
