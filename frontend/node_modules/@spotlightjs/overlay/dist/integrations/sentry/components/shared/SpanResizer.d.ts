import { ResizeDirection } from './Resizer';

type SpanResizerProps = {
    handleResize: (e: MouseEvent) => void;
    setIsResizing: (val: boolean) => void;
    isResizing: boolean;
    direction?: ResizeDirection;
};
export default function SpanResizer({ handleResize, isResizing, setIsResizing, direction, ...props }: SpanResizerProps): import("react/jsx-runtime").JSX.Element;
export {};
