import { FilterConfigs } from '../../hooks/useTraceFiltering';

interface TraceListFilterProps {
    searchQuery: string;
    setSearchQuery: (query: string) => void;
    activeFilters: string[];
    setActiveFilters: React.Dispatch<React.SetStateAction<string[]>>;
    filterConfigs: FilterConfigs;
}
export default function TraceListFilter({ searchQuery, setSearchQuery, activeFilters, setActiveFilters, filterConfigs, }: TraceListFilterProps): import("react/jsx-runtime").JSX.Element;
export {};
